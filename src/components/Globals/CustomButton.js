import React from "react";

const CustomButton = () => {
  return (
    <button
      style={{
        background: "#00c2cb",
        color: "white",
        padding: "15px",
        outline: "none",
        border: "none",
        borderRadius: "20px",
      }}
    >
      Submit
    </button>
  );
};

export default CustomButton;
